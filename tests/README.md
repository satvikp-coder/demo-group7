# Automated Test Suite (`tests/`)

**Status:** PLANNED

Directory for automated unit tests, component tests, API integration tests, and DSA algorithm performance benchmarks.

## Subdirectories
- `frontend/` — React UI component tests (Vitest / React Testing Library)
- `backend/` — Express REST API endpoint & database integration tests (Supertest / Jest)
- `dsa/` — DSA module correctness & time/space complexity benchmarks
