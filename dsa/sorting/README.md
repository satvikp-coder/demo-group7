# Sorting Module (`dsa/sorting/`)

**Implementation:** `MergeSorter.ts`  
**Description:** Pure TypeScript manual **Merge Sort** implementation ($O(N \log N)$ worst-case time complexity). Used for sorting hotels by price/rating and attractions by duration/rating. Satisfies academic requirements over generic `Array.prototype.sort()`.
