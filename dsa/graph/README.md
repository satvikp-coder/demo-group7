# Graph Module (`dsa/graph/`)

**Implementation:** `AdjacencyListGraph.ts`  
**Description:** Weighted, undirected Adjacency List graph representing one city's road network topology (hotel node + attraction nodes). Rebuilt fresh per city request in RAM ($O(V+E)$ space).
