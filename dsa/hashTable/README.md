# Hash Table Lookup Cache (`dsa/hashTable/`)

**Implementation:** `EntityHashTable.ts`  
**Description:** Application-level in-memory Hash Table ($O(1)$ average lookup time) providing instant access to confirmed city metadata, attractions, hotels, and restaurants.
