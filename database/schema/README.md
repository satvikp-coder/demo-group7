# Database Schema (`database/schema/`)

Contains `schema.sql` — full PostgreSQL DDL normalized to 3NF, including `uuid-ossp` and `pg_trgm` extensions, foreign key constraints (`ON DELETE CASCADE`), CHECK constraints, and B-tree / GIN indexes.
