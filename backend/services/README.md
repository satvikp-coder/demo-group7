# Backend Services

**Status:** NOT STARTED (Planned Next Phase)

- `itineraryService.js` — Orchestrates intra-city trip generation by calling `dsa/greedy` and `dsa/dijkstra`.
- `searchService.js` — Executes prefix search lookups by calling `dsa/trie`.
- `lookupService.js` — Provides fast entity indexing by calling `dsa/hashTable`.
